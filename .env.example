# Copy to .env.local. These values are for the local Supabase stack
# (npm run db:start); its keys are fixed defaults, not secrets.
# For Supabase Cloud, use the project's URL and publishable key instead.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH

# Public base URL of the app (used for auth redirects and emails)
NEXT_PUBLIC_SITE_URL=http://localhost:3200
