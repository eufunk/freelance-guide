## What does this change?

<!-- Short description. Which phase / TODO item does it belong to? -->

## Checklist

- [ ] Helps the user reach their first freelance client (or is required infrastructure)
- [ ] Business logic lives in `lib/` and has unit tests
- [ ] Content lives in `content/`, not in UI components
- [ ] Tested on mobile width (375 px) and desktop
- [ ] `npm run lint`, `npm run typecheck`, `npm test` pass
