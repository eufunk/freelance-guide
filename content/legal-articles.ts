import type { LegalArticleInput } from "@/lib/content/schema";

// Written in Phase 10. Every article needs sources and a lastVerified date.
export const legalArticles = [] satisfies LegalArticleInput[];
